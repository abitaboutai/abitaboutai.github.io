# Bomber Blog

GitHub Pages (Jekyll) blog scaffold.

## Local preview

```bash
bundle install
bundle exec jekyll serve
```

## Deploy
Push to GitHub and enable Pages (Settings → Pages → Deploy from branch).
